{
  "name": "ce-new-jsts-002-root",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev:frontend": "pnpm --filter frontend-client dev",
    "build:frontend": "pnpm --filter frontend-client build",
    "start:order": "pnpm --filter order-service start",
    "start:payment": "pnpm --filter payment-service start",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "typescript": "^5.5.2",
    "vitest": "^1.6.0",
    "@vitest/coverage-v8": "^1.6.0"
  }
}
