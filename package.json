{
  "name": "ce-new-jsts-006-root",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build:admin": "pnpm --filter admin-dashboard build",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "typescript": "^5.5.2",
    "vitest": "^1.6.0",
    "@vitest/coverage-v8": "^1.6.0"
  }
}
